// The Minutes web app this desktop shell points at — your deployed Railway URL,
// e.g. 'https://minutes-production-xxxx.up.railway.app'.
//
// Leaving the placeholder is fine: the app asks for the URL on first launch and
// remembers it. Fill this in only if you want the app to ship pre-pointed at
// your server (no first-run prompt).
module.exports = {
  DEFAULT_URL: 'https://minutes-production.up.railway.app',
};
